database:
  type: mysql:11.0
  disk: 1024
  configuration:
    schemas:
      - arcadia
    endpoints:
      mysql:
        default_schema: arcadia
        privileges:
          arcadia: admin

mongodb:
  type: mongodb:3.6
  disk: 512
  configuration:
    database:
      - ArcadiaCluster
